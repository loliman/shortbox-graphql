overwrite: true
schema: "../shortbox-contract/schema/shortbox.graphql"
generates:
  src/types/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
      contextType: "../core/server#Context"
